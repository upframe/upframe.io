import * as queries from './queries'
import * as mutations from './mutations'
import { useQuery } from '@apollo/react-hooks'
import { hasError } from '../api'

export { queries, mutations, useQuery, hasError }
